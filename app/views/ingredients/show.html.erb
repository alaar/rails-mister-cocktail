<h1>Details about <%= @ingredient.name %> </h1>

<p>Here are the cocktails that you can make with it:</p>

<ul>
  <% @ingredient.cocktails.each do |cocktail| %>
    <li>
      <%= link_to cocktail.name, cocktail_path(cocktail) %>
    </li>
  <% end %>

</ul>
